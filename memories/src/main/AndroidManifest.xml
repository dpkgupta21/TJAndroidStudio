<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.traveljar.memories"
    android:versionCode="7"
    android:versionName="1.6">

    <uses-sdk
        android:minSdkVersion="15"
        android:targetSdkVersion="21" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.RECEIVE_SMS" />
    <uses-permission android:name="android.permission.READ_SMS" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="com.example.memories.permission.C2D_MESSAGE" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <uses-feature android:name="android.hardware.camera" />

    <application
        android:name=".volley.AppController"
        android:allowBackup="true"
        android:largeHeap="true"
        android:icon="@drawable/jar"
        android:label="@string/app_name"
        android:theme="@style/myAppTheme">
        <activity
            android:name=".login.SplashScreen"
            
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".login.SignIn"
            
            android:label="@string/app_name" />
        <activity
            android:name=".login.SignUp"
            
            android:label="@string/app_name" />
        <activity
            android:name=".login.NumberVerificationActivity"
            
            android:label="@string/app_name" />
        <activity
            android:name=".login.NumberVerificationActivity2"
            
            android:label="@string/app_name" />
        <activity
            android:name=".BaseActivity"
            
            android:label="@string/app_name"/>
        <activity
            android:name=".checkin.CheckInPlacesList"
            
            android:label="@string/app_name" />
        <activity
            android:name=".checkin.CheckInPreview"
            
            android:label="@string/app_name" />
        <activity
            android:name=".checkin.CheckInFriendsList"
            
            android:label="@string/app_name" />
        <activity
            android:name="com.traveljar.memories.checkin.CheckinDetail"
            
            android:label="@string/app_name" />
        <activity
            android:name=".picture.PictureCapture"
            android:label="@string/app_name" />
        <activity
            android:name=".picture.PictureCaptureBaseActivity"
            android:label="@string/app_name" />
        <activity
            android:name=".moods.MoodCapture"
            
            android:label="@string/app_name" />
        <activity
            android:name=".audio.AudioCapture"
            
            android:label="@string/app_name" />
        <activity
            android:name=".gallery.GalleryAudios"
            
            android:label="@string/app_name" />
        <activity
            android:name=".picture.PictureDetail"
            
            android:label="@string/app_name" />
        <activity
            android:name=".picture.PicturePreview"
            
            android:label="@string/app_name" />
        <activity
            android:name=".gallery.GalleryPhotos"
            
            android:label="@string/app_name" />
        <activity
            android:name=".gallery.GalleryBaseActivity"
            
            android:label="@string/app_name"/>
        <activity
            android:name=".gallery.GalleryPhotoDetail"
            
            android:label="@string/app_name"
            android:theme="@android:style/Theme.NoTitleBar" />
        <activity
            android:name=".pastjourney.PastJourneyList"
            
            android:label="@string/app_name" />
        <activity
            android:name=".newjourney.SelectedFriendsList"
            
            android:label="@string/app_name" />
        <activity
            android:name=".newjourney.AllFriendsList"
            
            android:label="@string/app_name" />
        <activity
            android:name=".newjourney.NewJourneyDetail"
            
            android:label="@string/app_name" />
        <activity
            android:name=".currentjourney.CurrentJourneyBaseActivity"
            
            android:label="@string/app_name" />
        <activity
            android:name=".currentjourney.JourneyInfoFriendsList"
            
            android:label="@string/app_name" />
        <activity
            android:name=".newjourney.AddLap"
            
            android:label="@string/app_name" />
        <activity
            android:name=".newjourney.LapsList"
            
            android:label="@string/app_name" />
        <activity
            android:name=".newjourney.LocationList"
            
            android:label="@string/app_name" />
        <activity
            android:name=".profile.ProfileActivity"
            
            android:label="@string/app_name" />
        <activity
            android:name=".profile.ChangePassword"
            
            android:label="@string/app_name" />
        <activity
            android:name=".audio.AudioDetail"
            
            android:label="@string/app_name" />

        <activity
            android:name=".video.CaptureVideo"
            
            android:label="@string/app_name" />
        <activity
            android:name=".video.VideoDetail"
            
            android:label="@string/app_name" />
        <activity
            android:name=".video.VideoPreview"
            
            android:label="@string/app_name" />
        <activity
            android:name=".gallery.GalleryVideos"
            
            android:label="@string/app_name" />
        <activity
            android:name=".note.CreateNotes"
            
            android:label="@string/app_name" />
        <activity
            android:name="com.traveljar.memories.note.NoteDetail"
            
            android:label="@string/app_name" />
        <activity
            android:name=".gallery.GalleryNotes"
            
            android:label="@string/app_name" />
        <activity
            android:name=".moods.MoodSelectFriends"
            
            android:label="@string/app_name" />
        <activity
            android:name=".moods.MoodDetail"
            
            android:label="@string/app_name" />

        <activity
            android:name=".activejourney.ActivejourneyList"
            
            android:label="@string/app_name" />

        <activity
            android:name=".currentjourney.JourneyInfo"
            
            android:label="@string/app_name"/>

        <activity
            android:name=".picture.PictureFullScreen"
            
            android:label="@string/app_name" />

        <activity
            android:name=".settings.Settings"
            
            android:label="@string/app_name"/>

        <activity
            android:name=".settings.AccountSettings"
            
            android:label="@string/app_name"/>

        <activity
            android:name=".currentjourney.TimecapsulePlayer"
            android:screenOrientation="landscape"
            android:label="@string/app_name"/>

        <activity
            android:name=".media.CaptureMedia"
            android:label="@string/app_name"
            />

        <service
            android:name=".services.PullContactsService"
            android:exported="false" />

        <service
            android:name=".picture.UploadPictureService"
            android:exported="false" />

        <service
            android:name=".video.UploadVideoService"
            android:exported="false" />
        <service
            android:name=".services.MakeServerRequestsService"
            android:exported="false" />

        <!--To recive and read text messages -->
        <receiver
            android:name=".utility.SMSReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter android:priority="999">
                <action android:name="android.provider.Telephony.SMS_RECEIVED" />
            </intent-filter>
        </receiver>

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />


        <!--
          BroadcastReceiver that will receive intents from GCM
          services and handle them to the custom IntentService.
          The com.google.android.c2dm.permission.SEND permission is necessary
          so only GCM services can send data messages for the app.
        -->
        <receiver
            android:name=".gcm.GcmBroadcastReceiver"
            android:permission="com.google.android.c2dm.permission.SEND">
            <intent-filter>

                <!-- Receives the actual messages. -->
                <action android:name="com.google.android.c2dm.intent.RECEIVE" />
                <!-- Receives the registration id. -->
                <action android:name="com.google.android.c2dm.intent.REGISTRATION" />

                <category android:name="com.example.memories" />
            </intent-filter>
        </receiver>

        <receiver android:name="com.traveljar.memories.services.NetworkStateBroadCastReceiver"
            android:label="NetworkConnection">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE"/>
            </intent-filter>
        </receiver>

        <!--
          Application-specific subclass of GCMBaseIntentService that will
          handle received messages.
          By default, it must be named .GCMIntentService, unless the
          application uses a custom BroadcastReceiver that redefines its name.
        -->
        <service android:name=".gcm.GcmIntentService" />
    </application>

</manifest>